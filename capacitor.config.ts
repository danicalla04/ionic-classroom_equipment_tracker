import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.equipmenttracker',
  appName: 'Classroom Equipment Tracker',
  webDir: 'dist'
};

export default config;
